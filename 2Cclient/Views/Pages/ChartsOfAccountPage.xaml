<Page x:Class="_2Cclient.Views.Pages.ChartsOfAccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="План счетов"
                       Foreground="{StaticResource Text}"
                       FontSize="18"
                       FontWeight="SemiBold"/>
            <TextBlock Text="Только просмотр"
                       Foreground="{StaticResource Text2}"
                       Margin="0,6,0,0"/>
        </StackPanel>

        <!-- Tiles -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Margin="0,4,0,0"
                                   IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- square tile -->
                        <Border Width="200"
                                Height="200"
                                Margin="0,0,12,12"
                                Background="{StaticResource Panel2}"
                                BorderBrush="#243142"
                                BorderThickness="1"
                                CornerRadius="16"
                                Padding="14">

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- number -->
                                <Border Grid.Row="0"
                                        Background="#0F141B"
                                        CornerRadius="10"
                                        Padding="10,6"
                                        HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding NumChart}"
                                               Foreground="{StaticResource Text}"
                                               FontSize="16"
                                               FontWeight="SemiBold"/>
                                </Border>

                                <!-- name -->
                                <TextBlock Grid.Row="2"
                                           Text="{Binding Name}"
                                           Foreground="{StaticResource Text}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap"
                                           Margin="2,0,2,0"/>

                                <!-- subconto (hidden when empty) -->
                                <TextBlock Grid.Row="4"
                                           Text="{Binding SubcontoText}"
                                           Foreground="{StaticResource Text2}"
                                           TextWrapping="Wrap"
                                           Margin="2,0,2,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasSubconto}" Value="False">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Grid>
</Page>
