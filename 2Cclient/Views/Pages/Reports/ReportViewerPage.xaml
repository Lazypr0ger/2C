<Page x:Class="_2Cclient.Views.Pages.Reports.ReportViewerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Page.Resources>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#0F141B"/>
            <Setter Property="Foreground" Value="{StaticResource Text2}"/>
            <Setter Property="BorderBrush" Value="#243142"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="RowStyle" TargetType="ListViewItem">
            <Setter Property="Margin" Value="0,2,0,2"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                                Background="#0F141B"
                                BorderBrush="#243142"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="8">
                            <GridViewRowPresenter
                                Columns="{Binding RelativeSource={RelativeSource AncestorType=ListView}, Path=View.Columns}"
                                Content="{TemplateBinding Content}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#162235"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CellWrap" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <Style x:Key="TotalText" TargetType="TextBlock" BasedOn="{StaticResource CellWrap}">
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock x:Name="TitleText"
                           Foreground="{StaticResource Text}"
                           FontSize="18"
                           FontWeight="SemiBold"/>

                <!-- ✅ ВАЖНО: x:Name существует -->
                <TextBlock x:Name="PeriodText"
                           Foreground="{StaticResource Text2}"
                           Margin="0,6,0,0"/>

                <!-- ✅ ВАЖНО: x:Name существует -->
                <TextBlock x:Name="ExtraLineText"
                           Foreground="{StaticResource Text2}"
                           Margin="0,6,0,0"
                           Visibility="Collapsed"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Обновить"
                        Style="{StaticResource ToolbarButtonBase}"
                        Click="Reload_Click"/>
            </StackPanel>
        </Grid>

        <!-- Viewer card -->
        <Border Grid.Row="2"
                Background="#10151C"
                CornerRadius="14"
                Padding="12">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Табличная часть"
                           Foreground="{StaticResource Text2}"/>

                <!-- ✅ ВАЖНО: x:Name существует -->
                <ContentControl x:Name="TableHost" Grid.Row="2"/>

                <!-- Totals -->
                <Border Grid.Row="4"
                        Background="#0F141B"
                        BorderBrush="#243142"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Итого" Style="{StaticResource TotalText}"/>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <!-- ✅ ВАЖНО: x:Name существуют -->
                            <TextBlock x:Name="Total1Text"
                                       Style="{StaticResource TotalText}"
                                       Margin="0,0,18,0"/>
                            <TextBlock x:Name="Total2Text"
                                       Style="{StaticResource TotalText}"
                                       Margin="0,0,18,0"/>
                            <TextBlock x:Name="Total3Text"
                                       Style="{StaticResource TotalText}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Button Grid.Row="4"
                Content="Назад"
                Height="40"
                Style="{StaticResource ToolbarButtonBase}"
                Click="Back_Click"/>
    </Grid>
</Page>
